name: analytics
description: "A new Flutter package project."
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=1.17.0"



dependencies:
  flutter:
    sdk: flutter

  firebase_analytics: ^10.8.0
  adjust_sdk: ^4.37.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
